const { resolve } = require('node:path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.cjs'), // vai dizer para o sequelize onde está nossa pasta de config
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //Vai dizer Onde está nossas Migrations
  'models-path': resolve(__dirname, 'src', 'app', 'models'), //Vai dizer Onde está as Models
};
